# Copy to .env and fill in. Never commit .env — see .gitignore.

NODE_ENV=development

# --- App -----------------------------------------------------------------
PUBLIC_APP_URL=http://localhost:3000

# --- Database (Phase 3 — required for auth to function) ------------------
DATABASE_URL=postgresql://neondb_owner:npg_sC8ScfxTw3Ha@ep-wispy-bread-aye50enp-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# --- Auth (Phase 3 — better-auth, ADR-007) --------------------------------
# Generate with: openssl rand -base64 32
BETTER_AUTH_API_KEY=ba_lsx25nwhnrcphc1a6h6nredcu4xhdpj7
# --- Redis (Phase 7/8 — realtime chat & viewer counts, ADR-006) ----------
REDIS_URL=redis://localhost:6379

# --- Cloudflare Stream (Phase 7 — video, ADR-005) -------------------------
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_STREAM_API_TOKEN=

# --- Cloudflare R2 (Phase 6/9 — object storage, avatars/thumbnails) -------
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET=

# --- Observability (Phase 12 — error tracking) ----------------------------
SENTRY_DSN=


# AUTH KEYS
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
